{
  "name": "@luxfi/evm-contracts",
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "@types/chai": "^5.2.3",
    "@types/mocha": "^10.0.10",
    "@types/node": "26.4.0",
    "chai": "^6.2.2",
    "ds-test": "https://github.com/dapphub/ds-test.git",
    "hardhat": "^3.15.0",
    "prettier": "^3.9.6",
    "prettier-plugin-solidity": "^2.4.1",
    "ts-node": "^10.9.2",
    "typescript": "7.0.2"
  },
  "version": "1.2.2",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/luxfi/evm.git",
    "directory": "contracts"
  },
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "rm -rf dist/ && npx hardhat compile && npx tsc -b ",
    "compile": "npx hardhat compile",
    "console": "npx hardhat console",
    "test": "npx hardhat test",
    "lint": "prettier --list-different 'contracts/**/*.sol'",
    "prepublish": "npm run build",
    "release:prepare": "rm -rf ./node_modules && npm install && npm run build"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  },
  "engines": {
    "npm": ">7.0.0",
    "node": ">=20.13.0"
  }
}
